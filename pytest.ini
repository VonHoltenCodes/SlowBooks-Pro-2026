[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -ra
    --strict-markers
    --disable-warnings
markers =
    integration: tests requiring real system binaries (tesseract, poppler-utils)
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
